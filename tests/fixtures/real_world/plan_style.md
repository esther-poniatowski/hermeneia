# Plan

| Step | Detail |
| --- | --- |
| setup | The preparation sequence defines baseline inputs and keeps a long sentence that exceeds the threshold for sentence-level analysis.<br>The evaluation branch tracks error signals and preserves one additional long sentence for table-cell parsing checks. |

> [!NOTE] This callout summary keeps enough words to exceed the sentence threshold while preserving focused phrasing.

If the baseline fails, the variant stalls.
If the baseline fails, why does the variant stall?
The baseline converges, the variant diverges under perturbation.
The baseline converges, whereas the variant diverges under perturbation.
